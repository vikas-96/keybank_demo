
    {{--
    @if(in_array($template_id,config('list.plot.plot_amenities')))
    <div class="col-md-12">
        <span>Plot amenities</span>
        <strong>{{ isset($value['plot']['plot_amenities']) ? implode(', ',$value['plot']['plot_amenities']):"" }}</strong>
    </div> 
    @endif 

    @if(in_array($template_id,config('list.plot.approved_land_use')))
    <div class="col-md-6">
        <span>Approved land use</span>
        <strong>{{ ($value['plot']['approved_land_use']) ?? null }}</strong>
    </div>
    @endif
    --}}



